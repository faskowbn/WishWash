/**
 * Created by brad on 3/31/2017.
 */
/*
 import React from 'react';

 import Order from './order';

 let OrderList = React.createClass({
 render() {
 console.log(this.props.orders);
 var orders = this.props.orders.map(function(order) {
 return <Order
 key={order.orderId}
 order={order} />
 });

 if (orders.length === 0) {
 orders = (<h1 className="no-orders center-text">
 Uh oh! You don't have any orders yet!
 </h1>);
 }
 return (
 <div className="order-list">
 { orders }
 </div>
 );
 }
 });
 export default OrderList;
 */